{
  "name": "deepseek-harness-linux-root",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix app/frontend run dev",
    "build": "npm --prefix app/frontend run build",
    "tauri": "npx tauri",
    "tauri:dev": "npm --prefix app/frontend run tauri:dev",
    "tauri:build": "npm --prefix app/frontend run tauri:build",
    "check": "cargo check && cargo test",
    "test": "npm --prefix app/frontend run build && cargo test"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
